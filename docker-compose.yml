version: '3'
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes: 
      - '.:/usr/src/app'
      - '/usr/src/app/node_modules'
    ports:
     - "3000:3000"
  cms:
    image: "agentejo/cockpit:latest"
    ports:
      - "8080:80"